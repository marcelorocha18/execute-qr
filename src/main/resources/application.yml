server:
  port: 9497

logging:
  pattern:
    console: >
      {
        "time":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSX}",
        "level":"%p",
        "traceId":"%X{traceId}",
        "logger":"%c",
        "message":"%m"
      }%n